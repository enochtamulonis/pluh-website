<div class="bg-gradient-to-r from-blue-700 via-indigo-400 to-indigo-500 h-[100vh]">
  <div class="flex flex-col items-center pt-40 w-full">
    <h1 class="font-bold text-7xl font-main text-indigo-100">PLUH GANG</h1>
    <p class="text-4xl text-gray-100 mt-16 text-center font-digital"> Friday 13th, Oct 2k23 </p>

    <div class="grid gap-4 mt-16">
      <% @songs.each.with_index do |song, index| %>
        <div class="text-2xl text-gray-200 font-pro"> <%= index + 1 %>. <%= song %> </div>
      <% end %>
    </div>
  </div>
</div>
